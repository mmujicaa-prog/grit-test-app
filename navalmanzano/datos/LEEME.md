# datos/

Esta carpeta contiene datos reales de inversores: **no se versiona**.

Solo están versionados este fichero y `padron_ejemplo.csv` (datos ficticios para pruebas).

El padrón real se descarga en cada sesión desde Google Drive (ver `config/ajustes.yaml`).
